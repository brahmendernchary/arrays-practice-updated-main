const arr = [1, 2, 3, "naga"];
// console.log(arr[0])

arr.push[4];
// arr.unshift(0)//to add first element in array
// arr.pop()//to remove last element
arr.shift(); //to remove first element
for (const item of arr) {
  console.log(item);
}
